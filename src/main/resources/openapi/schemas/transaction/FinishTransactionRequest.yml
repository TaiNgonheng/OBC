type: object
properties:
  initRefNumber:
    type: string
    description: Reference number must be in 32 digits
  otpCode:
    type: string
    description: OTP code must be in 6 digits
  key:
    type: string
    description: Key must be at least 1 digit and max of 64 digits