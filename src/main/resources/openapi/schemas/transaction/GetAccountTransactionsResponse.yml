allOf:
  - $ref: '../common/ResponseWrapper.yml'
  - type: object
    properties:
      data:
        type: object
        properties:
          transactions:
            type: array
            items:
              $ref: './TransactionHistoryModel.yml'
          totalElement:
            type: integer
            format: int64
