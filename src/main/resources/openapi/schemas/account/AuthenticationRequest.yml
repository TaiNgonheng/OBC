type: object
description: Authentication request
required:
  - loginType
  - login
  - key
properties:
  loginType:
    $ref: '../common/LoginTypeEnum.yml'
  login:
    type: string
    maxLength: 30
  key:
    type: string
    pattern: ^[a-fA-F0-9]{64}$